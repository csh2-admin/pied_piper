# .streamlit/secrets.toml
# ─────────────────────────────────────────────────────────────
# FOR LOCAL USE ONLY — never commit this file to git.
# On Streamlit Community Cloud, set these in the Secrets panel
# (App Settings → Secrets) instead of this file.
# ─────────────────────────────────────────────────────────────

ANTHROPIC_API_KEY = "YOUR_ANTHROPIC_API_KEY"
DB_URI            = "postgres://tsdbadmin:PASSWORD@YOUR_HOST.tsdb.cloud:PORT/tsdb"
